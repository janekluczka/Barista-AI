package com.luczka.baristaai

import androidx.activity.ComponentActivity

/**
 * Empty Activity for Compose UI tests. Does not call [androidx.activity.compose.setContent] in onCreate,
 * so tests can call [androidx.compose.ui.platform.setContent] on the test rule.
 */
class EmptyComposeActivity : ComponentActivity()
